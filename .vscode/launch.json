{

    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome on localhost:5100",
            "url": "http://localhost:5100",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
